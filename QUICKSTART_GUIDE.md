# 🚀 快速启动指南

## 前提条件

- Python 3.9+
- Node.js 16+
- npm 或 yarn

## 后端启动

```bash
cd backend-python

# 首次运行：安装依赖
./setup.sh

# 启动服务
./run.sh
```

后端将在 **http://localhost:3000** 启动

## 前端启动

```bash
cd frontend

# 首次运行：安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端将在 **http://localhost:5173** 启动

## 访问应用

1. 在浏览器打开 **http://localhost:5173**
2. 配置微信数据目录路径
3. 选择要查看的微信账号
4. 浏览聊天列表
5. 点击"查看"可以在新窗口查看聊天记录
6. 点击"数据分析"可以查看统计、词云、AI 分析

## API 文档

访问 **http://localhost:3000/docs** 查看完整的 Swagger API 文档

## 功能列表

### 基础功能
- ✅ 用户管理（扫描、解析、查询）
- ✅ 聊天列表（排序、预览、筛选）
- ✅ 消息查询（分页、日期范围）
- ✅ HTML 渲染（在线查看、无限滚动）

### 数据分析功能
- ✅ 聊天统计（消息数、类型分布、时间趋势）
- ✅ 用户活跃度分析（发言排名、活跃时段）
- ✅ 词频统计（jieba 分词）
- ✅ 词云生成（可视化）

### AI 功能（需要配置 OpenAI API Key）
- ✅ 聊天内容总结
- ✅ 情感分析
- ✅ 智能问答

## 配置 AI 功能（可选）

如果要使用 AI 功能，需要在后端配置 OpenAI API Key：

```bash
cd backend-python
cp .env.example .env
# 编辑 .env 文件，设置 OPENAI_API_KEY
```

## 常见问题

### Q: 后端启动失败？
A: 检查 Python 版本是否 >= 3.9，运行 `python3 --version`

### Q: 前端显示网络错误？
A: 确保后端已经启动并运行在 3000 端口

### Q: AI 功能无法使用？
A: 需要在后端 .env 文件中配置 OPENAI_API_KEY

### Q: 词云显示空白？
A: 检查聊天记录是否有足够的文本消息

## 开发模式

### 后端热重载
```bash
cd backend-python
source venv/bin/activate
uvicorn app.main:app --reload --port 3000
```

### 前端热重载
```bash
cd frontend
npm run dev
```

## 生产部署

### 后端
```bash
cd backend-python
source venv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 3000 --workers 4
```

### 前端
```bash
cd frontend
npm run build
# 将 dist 目录部署到 Web 服务器
```

## 技术支持

如遇到问题，请查看：
- `PYTHON_BACKEND_SUMMARY.md` - Python 后端开发总结
- `FINAL_SUMMARY.md` - 项目完成总结
- `backend-python/README.md` - 后端详细文档

祝使用愉快！🎉


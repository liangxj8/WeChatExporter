# 脚本使用说明

本目录包含 WeChatExporter 的自动化配置和运行脚本。

## 脚本列表

### 🔧 setup.sh - 环境配置脚本
自动下载 NW.js 并配置运行环境。

**功能特性：**
- ⚡ **原生支持** Intel (x64) 和 Apple Silicon (arm64)
- ⚡ 智能选择 NW.js 版本（Intel: v0.40.1, Apple Silicon: v0.80.0）
- ✅ 支持 Python 2.7 和 Python 3.x
- ✅ 支持新版 Node.js（自动从源码编译 sqlite3）
- ✅ 自动检测系统环境
- ✅ 智能配置依赖
- ✅ 自动更新项目配置文件

**使用方法：**
```bash
./scripts/setup.sh
```

### 🚀 run.sh - 应用运行脚本
启动 WeChatExporter 应用。

**使用方法：**
```bash
./scripts/run.sh
```

### 🧹 clean.sh - 清理脚本
清理安装过程中的临时文件和依赖，用于重新配置环境。

**使用方法：**
```bash
./scripts/clean.sh
```

## 快速开始

### 首次安装

```bash
# 1. 运行环境配置
./scripts/setup.sh

# 2. 启动应用
./scripts/run.sh
```

### 遇到问题时

```bash
# 1. 清理环境
./scripts/clean.sh

# 2. 重新配置
./scripts/setup.sh
```

## Apple Silicon (M1/M2/M3/M4) 用户

如果你使用的是 Apple Silicon Mac：

1. ⚡ **原生 arm64 支持**：无需 Rosetta 2，性能更优
2. ⚡ 自动下载 NW.js v0.80.0 arm64 原生版本
3. ⚡ sqlite3 自动编译为 arm64 native 版本
4. ⚡ 完全原生运行，功耗更低

### 确保已安装 Xcode Command Line Tools

```bash
xcode-select --install
```

## 常见问题

### Q: setup.sh 提示 "python: command not found"
A: 脚本已修复此问题，会自动使用 python3 并创建符号链接。

### Q: sqlite3 编译失败
A: 确保已安装 Xcode Command Line Tools：
```bash
xcode-select --install
```

### Q: Node.js 版本太新
A: 脚本会自动检测并从源码编译 sqlite3，支持最新版 Node.js。

### Q: 如何完全重置环境
A: 运行清理脚本后重新配置：
```bash
./scripts/clean.sh
./scripts/setup.sh
```

## 技术说明

### 编译策略

脚本会根据系统环境智能选择最佳策略：

1. **Intel Mac**: 
   - 使用 NW.js v0.40.1 (x64)
   - 优先使用预编译的 sqlite3 模块
   - 如需要则从源码编译

2. **Apple Silicon**: 
   - 使用 NW.js v0.80.0 (arm64 原生)
   - 从源码编译 sqlite3 (arm64 原生)
   - 完全原生运行，无需 Rosetta 2

### 目录结构

```
WeChatExporter/
├── scripts/
│   ├── setup.sh      # 环境配置
│   ├── run.sh        # 运行应用
│   ├── clean.sh      # 清理环境
│   └── README.md     # 本文档
├── nwjs/             # NW.js 运行时（自动下载）
└── development/
    ├── node_modules/ # Node.js 依赖
    └── .python-link/ # Python 符号链接（自动创建）
```

## 更多帮助

详细的安装和使用说明，请参考项目根目录下的 `INSTALL.md` 文件。

